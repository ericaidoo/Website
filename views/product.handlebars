
<!doctype html>

<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>SV - Products</title>
		<meta name="description" content="form to add new books">
		<meta name="author" content="Mark Tyers">
		<link href="{{host}}/style.css" type="text/css" rel="stylesheet" />
		<script type="module" src="main.js"></script>
	</head>
	<body>
		<header>
		<h1>Products</h1>
		<a href="/logout">Log out</a>
		</header>
    
		{{#if msg}}
 			<aside>
				<p class="msg">{{msg}}</p>
			</aside>
		{{/if}}
		<main>
    	<form action="/sv" enctype="multipart/form-data" method="post">
			<p>
				<label for="item">Item ID: </label><br />
				<input type="number" name="itemID" value="">
		</form> 
      <div>
        <p>SHOPPING LIST : </p>
        
<table>
         {{#each all}}
      <tr>
        <td> {{this.name}}</td>
        <td> {{this.price}}</td>
       </tr> 
      {{/each}}
</table>
       
     <p>Total : {{total}}</p>
      </div>
      
		</main>
     <a class="add" herf="/sv/add">Add Product</a>
    </body>
</html>
